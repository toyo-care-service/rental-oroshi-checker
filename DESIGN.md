# レンタル卸 金額不一致チェッカー 設計書 v3.1

3回の設計レビュー（MUST 計20件）を反映した版。v3 の中心は **「会計バケット」と「フラグ」の二層化**（10章）。

## 1. 目的

福祉用具レンタル卸元（以下「卸元」）から届く月次の請求データと、社内の販売管理システム（以下「基幹システム」）から出力した支払予定表を突合し、**金額が一致しない利用者の一覧**を出す。

現状は700〜900行を目視で照合している。基幹システムの入力画面は顧客番号・顧客名がコピーできず、検索も CSV 出力もできない。

## 2. 利用者と運用

| | |
|---|---|
| 主な利用者 | 経理担当（現場）1〜数名 |
| 頻度 | 月1回 |
| 端末 | Windows PC |
| 用途 | ①卸元への金額確認 ②基幹システムへの料金変更入力 |

## 3. 形態

**GitHub Pages 上の静的アプリ**（`index.html` ＋ 同梱ライブラリ。ビルド不要）。

最重要要件は「利用者の氏名を含むデータを外部に送信しないこと」。サーバーを持たない静的サイトなら送信経路が存在しない。

### 3.1 外部 CDN を使わない

CDN 上のスクリプトは読み込んだファイルの内容にアクセスでき、外部送信できる。バージョン固定は改ざん対策にならない。

- ライブラリはリポジトリに同梱し、同一オリジンから読み込む（`vendor/` に配置。出所とSHA256を `vendor/README.md` に記録）
- `<meta http-equiv="Content-Security-Policy">` で **`default-src 'self'; connect-src 'none'; form-action 'none'; object-src 'none'; base-uri 'none'`** を宣言
- Shift_JIS のデコードはブラウザ標準の `TextDecoder('shift_jis')` を使い、外部ライブラリを足さない

## 4. データフロー

```
基幹システム出力ファイル ─┐
                          ├→ ブラウザ内で読込・突合 → 画面表示 → xlsx をローカル保存
卸元の請求データファイル ─┘
```

ネットワーク送信は初回の HTML／同梱ライブラリの取得のみ。

## 5. 入力仕様

### 5.1 基幹システム側（支払予定表）

- 拡張子は `.xls` だが**実体は Shift_JIS のタブ区切りテキスト**。1行目がヘッダー、30列
- 値の一部が `"` で囲まれ、囲まれない列と混在する
- **引用符付きフィールド内に改行が入る行がある**。行分割ではなく CSV パーサで処理する
- **金額の比較には「決定借受料(税抜)」を使う**。「決定卸先料金」は税込のため使わない
- 対象年月は「年度」列から取得できる（例: `2026年8月`）

### 5.2 卸元側（請求データ）

**CSV（Shift_JIS、20列）** ※推奨
- 利用者コード／利用者名／利用者カナ／マーク／区分／伝票No／拠点／商品コード／商品名／型式／開始日／終了日／中断日／再開日／数量／単位／金額／税ほか
- マーク: `○`新規 `●`解約 `▲`中断 `□`差分
- 金額の負数は `▲2,200` 形式
- **対象年月を示す列が無い**（開始日は契約開始日であって請求月ではない）

**xlsx（旧形式、10列）**
- 利用者コード・拠点・マーク・終了日が無く、名寄せ精度と範囲チェックが落ちる。読込時に警告し CSV を推奨する
- 並べ替えでヘッダー行が最終行に紛れることがある。1列目「利用者名」かつ2列目「金額」の行は読み飛ばす

### 5.3 パース結果の検証

パースに失敗した値を**ゼロ扱いしない**。以下を検証し、失敗行はバケット `金額不明` に入れる。

- 文字コード（Shift_JIS として解釈できないバイト列）、UTF-8 BOM
- 引用符の不整合、列数の過不足
- 数値: 全角数字、円記号、`▲`／`△`／括弧による負数、空欄、`-`
- 日付として解釈できない開始日・終了日（**金額とは別のフラグとして扱う**。金額が読めれば検算には参加できる）

**レコードとして復元できない場合はファイル全体を読込不可として停止する。** 引用符が閉じない、区切り数が想定と大きく異なるなど、行の境界そのものが確定できない状態では、10.1 の「全行を必ず1バケットに入れる」が成立しない。部分的に処理して残りを黙って捨てるより、読み込ませない方が安全。停止時は該当箇所の行番号と前後を提示する。

### 5.4 対象年月の確認（機械では検証できない）

**卸元の CSV には年月列が無いため、両ファイルが同じ月のものかを機械で判定できない。**

- 基幹システム側から読み取った年月を画面に大きく表示する
- 利用者が「卸元のデータも同じ年月である」を明示的にチェックするまで**突合を実行させない**
- 基幹システム側の年月が複数混在していれば実行不可
- 確認した年月は出力ファイルに記載する

前月と当月を突合しても差額の計算自体は成立するため、ここは人の確認に頼るしかない。だからこそ既定では進めない。

### 5.5 重複と取り違えの検出

- 同一の伝票No・商品コード・利用者・期間の完全重複を検出して**件数を提示する**。**自動では除外しない**（正当な複数明細があり得るため）
- 同じファイルを両方の欄に入れた場合は検出して止める

## 6. 突合範囲（対応表）

卸元の「拠点」と基幹システムの「部門 × 仕入先コード」は 1 対 1 ではなく、1つの部門×仕入先が複数拠点に対応することがある。

**対応表はアプリに埋め込まず、利用者が画面で登録する。** 公開リポジトリのコードに取引先名・仕入先コードを載せないため。

- 基幹システム側は「部門・仕入先コードを空欄で全件出力」を推奨。ファイル内の部門×仕入先の組を自動抽出し、登録候補として提示する
- 対応表に無い組の行は、バケット `範囲外` に入る

## 7. 利用者の名寄せ

### 7.1 単位

- 卸元側: **利用者コード**単位（CSV）。コードが無い xlsx 形式は (カナ, 氏名) を代用キーにする
- 基幹システム側: **顧客番号 × 氏名**単位（取次店は1つの顧客番号に複数の利用者がいる）

v1 の実装には、同じカナの別人がいると片方が上書きされて消える欠陥があった。実データにも、姓が同じで名の読みが一致する別人の組が複数あり（基幹システム側に同一カナで登録されている）、誤った突合結果を生んだ。単位を分けることで解消する。

### 7.2 段階的な対応づけ

氏名・カナを NFKC 正規化し、空白を除去。末尾のハイフンや `【…】` `(…)` を除去したうえで、次の順に処理する。

| 段階 | 条件 | 付与するフラグ | 実データでの人数 |
|---|---|---|---|
| ⓪ | **確認済み対応表**に登録済み | なし | 初回は0 |
| ① | カナと氏名の**両方**が一致 | なし | 約84% |
| ② | カナのみ一致 | `根拠:カナのみ` | 約6% |
| ③ | 氏名のみ一致 | `根拠:氏名のみ` | 約9% |
| ④ | 施設名のあいまい一致 | `根拠:施設名` | 約1% |

各段階で**1対1に確定できるものだけ**を対応づける。②〜④は自動で結ぶが、**必ずフラグを立て、両者の元の表記を出力に併記する。**

**②〜④で結んだうえに金額も食い違う組は `要確認(強)` とする。** 別人の金額がたまたま一致する確率は低いため、金額一致なら注記は弱く、金額不一致なら強く出す。

利用者が確認した組は**確認済み対応表**に登録でき、翌月からは段階⓪として扱われフラグが消える。

確認済み対応表は次を持つ。
- 卸元側の利用者コード（xlsx形式のときはカナ＋氏名）と、基幹システム側の顧客番号＋氏名
- **確認したときの元表記（両側）**、対応根拠（どの段階で結んだか）、確認日時
- 元表記が次回と変わっていた場合は自動適用せず、`要確認` に戻す（表記変更・誤登録の追跡）

### 7.3 1対多と曖昧

- 卸元側の複数単位が同じ基幹システム単位を指し、かつそれらの候補が他に無い場合は、**1人として合算**し `合算` フラグを立てて元の利用者名を併記する（卸元側で1人が複数の利用者コードに分かれているケース。実データにも複数あった）
- 候補が複数残る場合は**自動対応せず**、`名寄せ要確認` フラグを立てて理由（候補の一覧）とともに出力する
- どちらか片側にしか無い利用者も保持する

## 8. 明細のペアリング

### 8.1 文字列の正規化（実装を一意に決めるための定義）

**商品名**
1. NFKC 正規化 → 大文字化
2. 空白（半角・全角）と記号 `・･.,()（）/-_` を除去

**類似度**は正規化後の文字列の **2-gram（bigram）Dice 係数**を使う。

```
sim(A, B) = 2 * |bigram(A) ∩ bigram(B)| / (|bigram(A)| + |bigram(B)|)
```

多重集合ではなく集合として扱う。いずれかが1文字以下なら、完全一致で 1.0、それ以外 0。
（`SequenceMatcher` は実装系によって結果が変わるため使わない）

**型式**は NFKC 正規化 → 大文字化 → 英数字以外を除去。**4文字未満の型式は根拠に使わない。**

型式が商品名に含まれるかの判定は、**単純な部分文字列一致にしない。** 商品名から英数字の連続を token として切り出し、そのいずれかと**完全一致**するか、token が型式で始まる（色記号などの後置を許す）場合のみ「含まれる」とする。

> v1 では空白を先に除去したため `部品台H ABC-BH` が `HABCBH` になり、型式 `ABCBH` と一致せず誤判定を量産した。token 単位にすることで防ぐ。

### 8.2 判定規則

同一利用者の中で、明細どうしを**一対一の最適割当**で対応づける。v1 の貪欲法は入力順で結果が変わり、実データでも同点候補を持つ利用者が多数いた。利用者あたりの明細は最大10件程度なので厳密解で問題ない。

**候補として許すのは、金額以外の強い根拠が1つ以上ある組だけ。**
1. 型式が商品名の token と一致する（8.1 の定義）
2. 商品名の類似度が **0.45 以上**

**失格条件は設けない。** 期間・数量による失格を検討したが、
- 基幹システム側に**数量の列が存在しない**ため、数量の突き合わせは実行できない
- 卸元側には「7月度後半分」のような**前月分の調整行が当月請求に載る**ため、期間の不整合を失格にすると正当な行を落とす

したがって期間は失格ではなく**加点のみ**に使う。

**得点**

| 根拠 | 点 |
|---|---|
| 型式が token 一致 | 4 |
| 商品名の類似度 | 類似度 × 3 |
| 卸元の開始日と基幹システムの取引開始日が一致 | 1 |
| 金額一致 | 1（補助。単独では候補にならない） |

**目的関数**: 割り当てた組の得点合計を最大化する。**未対応のコストは 0**（対応づけないことを常に許す）。利用者あたり最大10件程度なので、分岐限定による厳密解で求める。

**次点の定義**: 最適解と**割当が1組でも異なる**解のうち、得点合計が最大のもの。未対応を選ぶ組合せも比較対象に含む。

**確定の条件**
- 最適解と次点の得点差が **0.5 以上** → 最適解の全組を確定
- 差が 0.5 未満 → **最適解と次点で割当が一致する組だけを確定し、食い違う組は未対応にする**

未対応が1件でも残る利用者には `明細説明未確定` フラグを立てる。

### 8.3 注意

- 卸元の型式欄にはメーカー型番ではなく卸元の自社品番（例 `870-140`）や、調整行の月度（例 `6月度前半分`）が入る
- **商品コードは両者で体系が異なり、実データでは1件も一致しなかった**ため突合キーに使えない

## 9. 判定単位

**利用者単位で合計を比較する。** 卸元が部材ごとに請求し、基幹システムが1行にまとめている商品があるため。

判定に使う金額は、後述のバケット **`突合済み` と `片側のみ` の合計**。`除外`・`範囲外`・`金額不明` は判定に入れない。

明細が対応づかなくても（`明細説明未確定`）、利用者合計の判定は行う。**合計の判定と、明細の説明可能性は別軸**として扱う。

## 10. 二層モデル（v3の中心）

v2 までは「区分」に判定・警告・表示状態が混在しており、排他でも網羅でもなかった。**会計バケットとフラグを分離する。**

### 10.1 会計バケット（排他・網羅）

**両ファイルの各行を、必ず1つだけのバケットに割り当てる。** どのバケットにも入らない行、2つに入る行が存在してはならない。判定の優先順位は上から。

| 優先 | バケット | 条件 |
|---|---|---|
| 1 | `金額不明` | 金額をパースできない |
| 2 | `範囲外` | 対応表に無い拠点／部門×仕入先 |
| 3 | `除外` | 利用者が指定した除外条件に合致（10.3） |
| 4 | `突合済み` | 相手側の行と対応づいた |
| 5 | `片側のみ` | 対応づかなかった |

卸元側・基幹システム側それぞれについて、5つのバケットの件数と金額を持つ。

### 10.2 フラグ（バケットとは独立の軸）

行や利用者に**複数付きうる**。会計には影響しない。

- `根拠:カナのみ` / `根拠:氏名のみ` / `根拠:施設名` / `合算` / `名寄せ要確認` / `要確認(強)`
- `明細説明未確定`
- `日付パース不能`
- `重複候補`
- 表示に関するもの: `しきい値未満で非表示` / `片側のみ非表示`

### 10.3 除外条件はデータ源ごとに定義する

商品コード体系が両者で異なるため、「商品コードで除外」は片側にしか効かない。**除外条件は卸元側・基幹システム側で別々に指定する。**

例（付属品の1円を対象外にする場合）
- 卸元側: 型式に該当の品番を含み、かつ金額が0か1
- 基幹システム側: 商品名に該当の品番を含み、かつ金額が0か1

品番はアプリに埋め込まず、利用者が画面で登録する。

除外した行数と金額は、**卸元側・基幹システム側を別々に**画面と出力に記載する。

**片側にしか除外条件が当たらなかった場合、もう片側の行は `片側のみ` として判定対象に残る**（意図した挙動）。両側を対象から外したいときは、両方に条件を書く必要がある。画面では、片側だけ除外された行数を警告として出す。

### 10.4 検算

**① 金額検算（バケットの等式）**

```
Σ基幹システム全行 − Σ卸元全行
  = (Σ基幹突合済み − Σ卸元突合済み)
  + (Σ基幹片側のみ − Σ卸元片側のみ)
  + (Σ基幹除外     − Σ卸元除外)
  + (Σ基幹範囲外   − Σ卸元範囲外)
```

各行が1つのバケットにしか入らないため、この等式は必ず成立する。**成立しなければ実装に取りこぼしがある。**

**② 金額不明があるときは検算不能**

金額をパースできない行の金額は「0」ではなく「不明」。`金額不明` が1件でもあれば、①の等式は成立しない。この場合は
- 「**金額検算は未成立**」と明示する
- 突合結果を**確定扱いにしない**
- 該当行を一覧で提示し、利用者が原因に対処できるようにする

**③ 表示との関係**

しきい値・非表示は**バケットの表示属性**であって会計には影響しない。画面には「表示している差額の合計」と「①の総差」を並べ、その差が `除外 + 範囲外 + しきい値未満で隠した分` で説明できることを示す。

### 10.5 「不一致なし」を出す条件

**次のいずれかが1件でも残る場合、「不一致なし」とは表示しない。「判定不能あり」と区別する。**

- `範囲外` / `金額不明` / `名寄せ要確認` / `明細説明未確定`
- **`根拠:カナのみ` / `根拠:氏名のみ` / `根拠:施設名`**

弱い根拠で結んだ利用者が残っている限り、別人を誤って結合したうえに金額が偶然一致し、「不一致なし」と表示される経路が残る。**確認済み対応表に昇格して根拠フラグが消えるまでは、確定扱いにしない。**

### 10.6 範囲もれの扱い

対応表でカバーされていない組が1件でもあれば、突合前に警告し、対象外になる行数と金額を明示する。続行はできるが、結果は「全件突合済み」ではなく「範囲外あり」と表示し、**出力ファイルの先頭にも恒久的に記載する。**

一度、複数ある拠点のうち1拠点だけで突合し、残りを丸ごと見落としかけた。

## 11. 設定の保存

`localStorage` に保存するのは**設定のみ**。業務データは保存しない。

- 対応表、確認済み対応表、除外条件、各切替の状態
- 保存は「この端末に保存する」の明示操作とし、消去ボタンを置く
- **保存内容はスキーマ検証する。** 壊れていれば読み込まず、警告して停止する
- **対応表が空の状態では突合を実行させない**（全行が範囲外になり「不一致なし」に近い結果が出るため）
- 設定の書き出し／読み込み（JSON）。版数と更新日時を持たせ、出力ファイルにも記載する
- 設定 JSON には拠点・部門・仕入先コードという内部情報が入る。取扱注意を画面に明記する

## 12. 出力仕様

1シート `金額不一致一覧`。**顧客番号の昇順**。

| 列 | 内容 |
|---|---|
| 顧客番号 / 顧客名 / 利用者名 | |
| 基幹システムの金額 / 卸元の請求 / 差額 | 利用者合計（`突合済み` + `片側のみ`） |
| 件数 | 不一致の明細数 |
| 不一致の内容 | `商品名：X円 → Y円` を改行区切りで最大8件 |
| 注記 | 名寄せの根拠フラグ、合算、明細説明未確定など |

**列の定義と表示ルールは1箇所の配列にまとめる**（指摘で差し替える前提のため）。

### 12.1 ヘッダーに残す情報

対象年月／入力ファイル名／読込日時／設定の版数／バケットごとの件数と金額／検算の成立可否／範囲外の有無。後日「なぜこの差額になったか」を追えるようにする。

### 12.2 名寄せの根拠を残す

②〜④で結んだ利用者は、**正規化前の元の表記を両側とも出力する**。末尾ハイフンや括弧書きを除去した結果だけを残すと、後から判断できない。

### 12.3 数式インジェクション対策

卸元由来の氏名・商品名・備考等が `=` `+` `-` `@` で始まる場合、Excel が式として解釈しうる。**セルは必ず文字列型で書き、該当する先頭文字はエスケープする。** 実データでも型式欄が `-` だけの行があった。

## 13. セキュリティ

- 公開URLだが、アプリ自体にデータを持たない
- コードに取引先名・仕入先コード・利用者情報を含めない
- 画面描画は `textContent` のみ。`innerHTML` を使わない。ファイル名の表示も同様
- 外部通信は CSP で禁止（3.1）

## 14. 非機能

- 想定データ量: 基幹システム 数千行 / 卸元 千行未満
- 突合は利用者単位に閉じるため計算量は実用範囲。最適割当も利用者あたり最大10件程度
- 処理中は進捗を表示する
- 対応ブラウザ: Chrome / Edge

## 15. 運用上の未決事項（アプリの動作には影響しない）

- 基幹システム側の出力を確定前に行うか、確定後に出し直すか
- 卸元側にある、利用者を特定できない行（施設のベッド一式など）の扱い

いずれもバケットとフラグで出力に残るため、判断が変わっても再実装は不要。
